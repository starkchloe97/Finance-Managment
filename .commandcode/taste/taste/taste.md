# Taste
- Wants the agent to survey the complete frontend/styling setup of the project before making UI changes, rather than jumping straight into edits. Confidence: 0.6
- For full-stack schema changes, prefers a backend/domain-first implementation sequence, followed by API/tests and then frontend wiring, rather than treating the work as a UI-only patch. Confidence: 0.95
- Prefers additive database migrations for schema evolution: preserve historical migrations, use new migrations for additions/removals, and explicitly document and implement legacy-data backfill before dropping columns. Confidence: 0.95
- Values repository-wide contract audits and explicit verification of downstream call sites when renaming or removing fields, with environment limitations reported separately from application failures. Confidence: 0.9
- Backend is the source of truth for business/financial calculations (e.g., job profit, cost, margin); the frontend renders backend-provided values only and never recomputes them client-side, and should not invent frontend-only statuses/enums that duplicate backend ones. Confidence: 0.85
- Keeps backend/API changes minimal in presentation/UX phases: preserve existing services, stores, and endpoints, and only add an endpoint/field where there is a genuine UX gap (don't build speculative endpoints). Confidence: 0.8
- Prefers one consolidated API endpoint over multiple round trips (e.g., a single `/dashboard` aggregate returning kpis, financial overview, job status, recent jobs, alerts) rather than many per-widget endpoints. Confidence: 0.8
- When providing an external instruction/specification file, expects its requirements to be implemented directly in the current project rather than merely reviewed or summarized. Confidence: 0.8
- Wraps all financial/double-entry mutations in `DB::transaction()` with `lockForUpdate()` on the relevant aggregate root (e.g., company capital account) to ensure concurrency-safe, ACID-compliant writes. Confidence: 0.85
- Surfaces domain invariant violations (e.g., insufficient available capital) through Laravel's `ValidationException::withMessages()` rather than custom exception classes, keeping business-rule errors in the same validation pipeline as input errors. Confidence: 0.75
- Separates distinct domain concepts into their own models/tables rather than overloading shared transaction tables — e.g., capital drafts get their own table/model rather than being mixed into `CompanyCapitalTransaction`, so planned/expected amounts never affect real financial balances. Confidence: 0.85
- Enforces explicit state-machine transitions on domain entities (e.g., a draft can only move draft → converted or draft → removed; attempting converted→convert, converted→remove, removed→convert, or removed→remove is rejected). Confidence: 0.8
- Uses append-only activity records for domain lifecycle events: each state transition (e.g., draft added/converted/removed) creates a new immutable record in a dedicated activity table, each with its own note and metadata, rather than overwriting the entity's historical fields or replacing earlier timeline entries. The entity's status field tracks current state, but historical activity records are never modified — every action appends a fresh audit entry. Confidence: 0.95
- Project targets MySQL as its database, so migrations must account for MySQL's 64-character foreign key constraint identifier limit — when table and column names are long, Laravel's auto-generated constraint name from `foreignId()->constrained()` can exceed the limit; use explicit `unsignedBigInteger()` + `foreign('column', 'short_name')` with a short custom constraint name instead. Confidence: 0.9
- Provides detailed, structured specifications in chat with explicit acceptance criteria, examples, and clearly delineated behavioral requirements, expecting the agent to implement directly against those criteria (including writing tests) rather than only summarizing them. Confidence: 0.85
- When consolidating or refactoring UI/forms, keeps the underlying business and financial logic unchanged — does not alter capital accounting, loan eligibility, or existing calculations, and reuses existing model/service/API logic rather than duplicating or modifying it. Confidence: 0.85
